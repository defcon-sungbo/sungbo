import 'package:flutter/material.dart';

class page404 extends StatelessWidget{
  @override
  Widget build(BuildContext context) {

    return new Scaffold(
      appBar: AppBar(title: const Text('Sungbo')),
      body: SafeArea(
        child: Text("미구현 된 기능니다."),
      ),
    );
  }

}